<h1>My Trips</h1>
<%= link_to "back", user_path(current_user) %>
You have <%= @trips.length %> trips!

<% @trips.each do |trip| %>
  <h2><%= link_to trip.location, trip_path(trip)%></h2>
 
<% end %>
  
